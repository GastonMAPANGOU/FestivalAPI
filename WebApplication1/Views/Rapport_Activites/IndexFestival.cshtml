@model IEnumerable<FestivalAPI.Models.Festival>

@{
    ViewData["Title"] = "Index";
}

<h1>Liste des régions</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nom)
                </td>
                <td>
                    <a asp-controller="Rapport_Activites" asp-action="Rapport_Activite_Festival_Graphe" asp-route-id="@item.IdF">Rapport Activité Region</a>
                </td>
            </tr>
        }
    </tbody>
</table>
